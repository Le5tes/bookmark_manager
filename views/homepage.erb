<h1> Welcome to Bookmark Manager!!!!!!! </h1>

<h3> Bookmarks: </h3>
<ul id = 'links'>
  <% @links.each do |link| %>
  <li>
  	<a href= <%= link.url %> > <%= link.title %> </a>
  </li>
  <% end %>
</ul>
